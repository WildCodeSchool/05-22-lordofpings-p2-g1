import GameHeader from './GameHeader'

const GameSkills = ({ hero }) => {
  // const [menu, setMenu] = useState(0)

  return (
    <div className='gameSkills'>
      <GameHeader hero={hero} />
      <div className='gladiator' onClick />
    </div>
  )
}

export default GameSkills
